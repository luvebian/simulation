**sim_spikes.ipynb:**
simulates spiky signal at one of the vertices, projects signal to the sensors, adds noise, reconstructs the signal, and estimates the error for different SNR values and vertix orientations. 
